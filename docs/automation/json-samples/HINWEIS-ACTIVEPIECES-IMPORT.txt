Import
  Nur: activepieces-import-skyos-webhook-starter.json (im Ordner-Root)
  Nicht: Dateien unter reference-http-payloads/ — das sind HTTP-Beispiele, kein AP-Template.

Die „7“ alten Dateien
  Liegen jetzt unter reference-http-payloads/ — bewusst getrennt vom importierbaren
  Template, damit klar ist: Copy-Paste / Tests, nicht „Import Flow“.

Vollstaendige Abdeckung der App
  Braucht nicht 7 importierte Workflows. Ein Master-Webhook-Flow + Router deckt
  die Agent-Modi; Reminder/Task/Note kommen optional per separater HTTP-API (siehe
  docs/workflow-http-api-activepieces.md).
